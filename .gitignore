# Self-host config: never commit your real values, copy .env.example instead.
.env.*.local

# OS / editor cruft
.DS_Store
.idea/
.vscode/
*.swp

# Build artefacts that don't belong in the repo (each app has its own
# .gitignore for finer-grained ignores).
node_modules/
vendor/
